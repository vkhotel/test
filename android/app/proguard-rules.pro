# AeroTouch release Proguard/R8 rules.
# Flutter's default rules already keep the engine + generated plugin registrant.
# Add app-specific keep rules here if you introduce reflection-based libraries.
