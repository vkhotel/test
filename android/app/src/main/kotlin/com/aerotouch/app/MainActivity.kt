package com.aerotouch.app

import io.flutter.embedding.android.FlutterActivity

/**
 * AeroTouch has no custom platform channels: every capability it needs
 * (sensors, sockets, storage, battery, haptics) is already covered by the
 * Flutter plugins declared in pubspec.yaml, so the activity is a thin,
 * unmodified [FlutterActivity].
 */
class MainActivity : FlutterActivity()
